{
    "name": "prism-starter",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "assets:build": "vite build",
        "assets:watch": "vite build --watch",
        "dev": "npm run assets:build && php vendor/bin/prism build",
        "build": "npm run assets:build && php vendor/bin/prism build production",
        "preview": "php vendor/bin/prism serve"
    },
    "devDependencies": {
        "autoprefixer": "*",
        "postcss": "*",
        "tailwindcss": "*",
        "vite": "*"
    }
}
